<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      * {
        margin: 0;
      }

      html,
      body {
        height: 100%;
      }

      body {
        line-height: 1.5;
        -webkit-font-smoothing: antialiased;
      }

      img,
      picture,
      video,
      canvas,
      svg {
        display: block;
        max-width: 100%;
      }

      input,
      button,
      textarea,
      select {
        font: inherit;
      }

      p,
      h1,
      h2,
      h3,
      h4,
      h5,
      h6 {
        overflow-wrap: break-word;
      }

      #root,
      #__next {
        isolation: isolate;
      }

      main {
        max-width: 720px;
        margin: 20px auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 30px;
      }
    </style>
    <title>Do a greeting</title>
  </head>
  <body>
    <main>
      <h2>Welcome</h2>
      <h1>{{ user_name }}</h1>
      <form>
        <input placeholder="Enter a message" type="text" name="message" />
        <button type="submit">Show Message</button>
      </form>
      <p>Here is the message you typed</p>
      <p>{{ message }}</p>
    </main>
  </body>
</html>
